<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Books List</title>
</head>
<body>
    <h1>Books List</h1>
    <ul>
        <% books.forEach(book => { %>
            <li><%= book.title %> by <%= book.author %> - <%= book.genre %> (Published on <%= book.published_date %>)</li>
        <% }); %>
    </ul>
    <a href="/books/new">Add a new book</a>
</body>
</html>
